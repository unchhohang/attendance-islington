@model IEnumerable<attendance_beta2.Models.teacher_course>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Courses.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teachers.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Courses.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teachers.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Teacher_Course_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Teacher_Course_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Teacher_Course_Id })
        </td>
    </tr>
}

</table>
