@model IEnumerable<attendance_beta2.Models.Semester_Routine>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Routines.Room)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Semesters.Level)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Routines.Room)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Semesters.Level)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SemesterRoutineId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SemesterRoutineId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SemesterRoutineId })
        </td>
    </tr>
}

</table>
